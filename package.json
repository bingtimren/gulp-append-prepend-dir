{
  "name": "gulp-append-prepend-dir",
  "version": "1.0.2",
  "description": "Append or prepend the file content with the corresponding file in a dir",
  "license": "MIT",
  "author": {
    "name": "bingtimren"
  },
  "bugs": {
    "url": "https://github.com/bingtimren/gulp-append-prepend-dir/issues"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/bingtimren/gulp-append-prepend-dir"
  },
  "main": "build/index.js",
  "types": "build/index.d.ts",
  "keywords": [
    "gulpplugin"
  ],
  "scripts": {
    "test": "tsc;mocha test/index.js",
    "cover": "tsc;istanbul cover _mocha -- ./test/* --recursive",
    "coveralls": "tsc;npm run cover -- --report lcovonly && cat ./coverage/lcov.info | coveralls"
  },
  "dependencies": {
    "through2": "^2.0.5"
  },
  "devDependencies": {
    "@types/through2": "^2.0.34",
    "@types/vinyl": "^2.0.3",
    "chai": "^4.2.0",
    "coveralls": "^3.0.3",
    "istanbul": "^0.4.4",
    "mocha": "^10.2.0",
    "mocha-lcov-reporter": "^1.2.0",
    "vinyl": "^1.2.0"
  }
}
